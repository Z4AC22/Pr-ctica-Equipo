# Pr-ctica-Equipo

<img src="https://definicion.de/wp-content/uploads/2013/06/positivo.png" width="250" height="200" >


```
nombre = input("Escribe tu nombre: ")  sirve para pedir un dato en este caso el nombre 
```
```
apellido_pat = input("Escribe tu apellido paterno: ") sirve para pedir un dato en este caso el apellido paterno 
```
```
apellido_mat= input("Escribe tu apellido materno: ") sirve para pedir un dato en este caso el apellido materno
```
```
edad = input("Escribe tu edad: ") sirve para pedir un dato en este caso es la edad
```
```
a=nombre[0: 1] esta linea crea una variable que llama a otra variable la cual solo vamos a ocupar la primera letra del valor que se le dio a la variable
```
```
b=apellido_pat[0: 1] esta linea crea una variable que llama a otra variable la cual solo vamos a ocupar la primera letra del valor que se le dio a la variable
```
```
c=apellido_mat[0: 1] esta linea crea una variable que llama a otra variable la cual solo vamos a ocupar la primera letra del valor que se le dio a la variable
```
```
d=edad[0: 2] esta linea crea una variable que llama a otra variable la cual solo vamos a ocupar la primera letra del valor que se le dio a la variable
```
```
x=apellido_mat[0:3] esta linea crea una variable que llama a otra variable la cual solo vamos a ocupar la primera letra del valor que se le dio a la variable
```
```
curp = print("Tu CURP es: ",a + d +c + b + x) esta linea es un print el cual va imprimir la suma de las variables 
```
